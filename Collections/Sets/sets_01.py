#unorderd collections of unique elements
thisset = {"apple", "banana", "cherry", "apple"}
print(thisset)

# len of a set
print(len(thisset))

# set can only holds strings, integers and booleans

state = 'Mississippi'
state_set = set(state)
print(state_set)