#unorderd collections of unique elements
thisset = {"apple", "banana", "cherry", "apple"}
print(thisset)

# len of a set
print(len(thisset))

# set can only holds strings, integers and booleans

# turn something into a set by using set()
state = 'Mississippi'
state_set = set(state)
print(state_set)

# add to a set
thisset.add('orange')